"""
An algorithm that selects the best data compressor predicting already known values.
"""

from algorithm import Algorithm
from metric import Metric


def select(sample_generator, algorithms, metric, h):
    pass
