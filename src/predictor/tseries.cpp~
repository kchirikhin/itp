#include "word.h"

/*void itp::Word::push_back(Symbol_t s) {
  word.push_back(s);
}

size_t itp::Word::size() const {
  return word.size();
}

void itp::Word::clear() {
  word.clear();
}

itp::Symbol_t& itp::Word::operator[](size_t pos) {
  return word[pos];
}

const itp::Symbol_t& itp::Word::operator[](size_t pos) const {
  return word[pos];
}

std::ostream & itp::operator << (std::ostream &ost, const Word &w) {
  for (size_t i = 0; i < w.size(); ++i) {
    ost << w[i];
  }

  return ost;
  }*/
